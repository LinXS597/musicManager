<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.zucc.musicmanager.mapper.WordCountMapper">
    <!--    <select id="getUser" resultType="com.test.dto.User">-->
    <!--        select * from user-->
    <!--    </select>-->
    <!--    <delete id="deleteUser" parameterType="Integer">-->
    <!--        delete from user where id =#{id}-->
    <!--    </delete>-->
    <!--    <insert id="addUser" parameterType="com.test.dto.User">-->
    <!--        insert into user(id,name,age,sex)values(#{id},#{name},#{age},#{sex})-->
    <!--    </insert>-->
    <select id="getLyricWC" resultType="cn.edu.zucc.musicmanager.model.WordCountInfo">
        select * from tbl_lyricwordcount order by value DESC limit 500
    </select>
    <select id="getSignatureWC" resultType="cn.edu.zucc.musicmanager.model.WordCountInfo">
        select * from tbl_signaturewordCloud order by value DESC limit 500
    </select>
    <select id="getPlaydescriptionWC" resultType="cn.edu.zucc.musicmanager.model.WordCountInfo">
        select * from tbl_playdescription order by value DESC limit 500
    </select>
</mapper>