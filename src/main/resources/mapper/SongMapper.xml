<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.zucc.musicmanager.mapper.SongMapper">
    <!--    <select id="getUser" resultType="com.test.dto.User">-->
    <!--        select * from user-->
    <!--    </select>-->
    <!--    <delete id="deleteUser" parameterType="Integer">-->
    <!--        delete from user where id =#{id}-->
    <!--    </delete>-->
    <!--    <insert id="addUser" parameterType="com.test.dto.User">-->
    <!--        insert into user(id,name,age,sex)values(#{id},#{name},#{age},#{sex})-->
    <!--    </insert>-->
    <select id="getAllLyric" parameterType="Integer" resultType="java.lang.String">
        select songlyric from tbl_songinfo limit 100 offset #{offset}
    </select>
    <select id="getCommentsendtime" resultType="Long">
        select sendtime from tbl_songcomment limit 100000
    </select>
    <select id="getHotComment" parameterType="Integer" resultType="cn.edu.zucc.musicmanager.model.SongComment">
        select * from tbl_songcomment GROUP BY commentid ORDER BY likecount DESC limit #{limit}
    </select>
</mapper>